# Build brief — cmd-037 (paste-ready for Grok Build)

**Host:** windows-new-hp (NEW-HP-LAPTOP)  
**Checkout:** `C:\Users\n8vet\OneDrive\Documents\GitHub\mscc-station`  
**Role:** WPF **client** — `RemoteMicSender` (mic-send pre-fill / cushion only)  
**Call the user Stew.**  
**Do not git push.** Commit on this host when done.  
**Do not touch Pi, `linux/`, `rpi/`, Avalonia, cmd-034, Windows `sdrcore-trans` / local TX gate from cmd-035, or `MsccRemotePhones`.**

---

## Problem

cmd-036 (`d26c7a7`, Client **9.23.0**) shipped paced send: WaveIn enqueues; `MsccMicTx` blocks for 480 frames then UDP; 500 ms ring; eight 10 ms WaveIn buffers. **Huge SA improvement.** WSPR full TX clean. **FT8 / JT9 / JT65** still show intermittent skirt pulses (every few seconds to much longer within a ~1 min TX).

**Build note (memory, not overriding Stew/Overseer):** the 500 ms ring is drained to the next packet — it is **not** a standing cushion. A late WaveIn callback still opens a hole on the wire; Pi **hold-last** paints the remaining bump. Recommended harden: keep **~60 ms** queued and ship from that cushion across a late callback, still on the capture clock (~60 ms extra delay). Stew left the cushion size to Overseer — **LOCKED: 60 ms** (not halfway).

cmd-035 local Win gate stays done. Pi / cmd-034 stay parked.

---

## Survey anchors (Overseer — post cmd-036 — implement against these)

| Item | Where / value |
|------|----------------|
| **WPF class** | `mscc-ui/windows-work-tree/mscc-mscc/mscc-new/src/MSCC.Wpf/RemoteAudio/RemoteMicSender.cs` |
| **Caller** | `.../RemoteAudio/RemoteAfEngine.cs` |
| **Ring** | `short[FramesPerPacket * 50]` = **500 ms** @ 48 kHz; `_ringRead` / `_ringWrite` / `_ringCount` |
| **WaveIn** | `BufferMilliseconds = 10`, `NumberOfBuffers = 8` (80 ms driver queue) |
| **SendLoop today** | `Monitor.Wait` until `_ringCount >= FramesPerPacket` (480), dequeue 480, one MSA1 UDP; no start pre-fill |
| **Overflow** | If enqueue would exceed ring length: drop whole callback, `_ringDrops += frames` (do not block WaveIn) |
| **Underrun** | Send thread blocks forever waiting for 480 — **no underrun counter / silence / skip** yet |
| **Log cadence** | First packet and every 500: `Mic TX N pkts → ep peak= drops=` (resets peak + drops) |
| **Packet / port** | `FramesPerPacket = 480`; MSA1 mono s16; `DefaultTxPort = 9101` — **do not change** |
| **Sibling (out of scope)** | `mscc-remote-audio/MsccRemotePhones/Audio/RemoteMicSender.cs` |

---

## Implement (hypothesis — labeled)

**Change stays in WPF `RemoteMicSender` only.**

1. **60 ms pre-fill / start cushion** before the **first** UDP send after `Start` (and after any intentional restart of the send path): wait until the ring holds **>= ~60 ms** of mono PCM — **~2880 samples @ 48 kHz**, or **6x480 frames** — before sending the first packet.
2. After that first send, continue **10 ms paced** sends as today: wait for >=480 (or keep a standing >=60 ms target if that is the clean way to hold the cushion — either is fine if wire stays capture-clock paced and MSA1 unchanged). Prefer a clear "armed after pre-fill" flag so Stop/Start resets the cushion.
3. **On underrun** (sender waiting, not enough samples for the next 480): prefer **silence packet** or **skip send** — **do NOT** invent smear / hold-last on the client. Increment an **underrun** counter.
4. Keep existing **overflow drop** behavior; count overflows/drops as today.
5. Extend the existing log cadence so Stew/Build can correlate with SA pulses — e.g. include `underruns=` (and keep `drops=` / `peak=`) on the first + every-500 packet log lines.
6. Do **not** change MSA1 framing, port **9101**, or Pi host / receiver.

### Suggested shape (non-binding)

- Constant e.g. `PrefillFrames = FramesPerPacket * 6` (2880) or `PrefillMilliseconds = 60`.
- `SendLoop`: before first send, wait until `_ringCount >= PrefillFrames`; then drain/send 480 as today.
- Optional standing cushion: after each send, only send when `_ringCount >= PrefillFrames` again (keeps ~60 ms queued) — if you do this, document the extra ~60 ms delay; still one 480-frame packet per tick when cushion allows.
- Underrun path: if wait times out or cushion collapses mid-TX, send silence MSA1 or skip; count it; never hold-last on client.
- Preserve: device list, volume-in-callback, stereo->mono max-abs, host resolve, AboveNormal `MsccMicTx`, eight 10 ms WaveIn buffers, 500 ms ring size (unless you need a slightly larger ring for headroom — only if required).

### Do NOT

- Change Pi host fill / hold-last / `remote_mic.c` (any tree).
- Reopen cmd-034 or cmd-035.
- Edit Avalonia `LinuxMicSender` or `MsccRemotePhones` RemoteMicSender.
- Change protocol, sample rate, frames/packet, or port 9101.

---

## Version / build

- Rebuild/install WPF client Stew runs (typically `C:\mscc-net9\` / house Windows client drop).
- Version: bump via existing house style (`ClientVersion.txt` / calendar `M.D.I`). Currently **9.23.0** — expect **9.23.1** (same day next iteration) unless the bump script says otherwise.

---

## Smoke

1. **Required:** Win11 client -> Pi host **Remote Digital** — **FT8 CQ** plus longer **JT65 / JT9** TX: intermittent skirt pulses **gone or clearly reduced**.
2. **Required:** **WSPR** still clean for a full TX.
3. **Regress:** quick **local** Digital TUNE/TX — cmd-035 key-up gate still OK.

---

## Status / commit

- ACK in `.mscc-coord/status/windows-new-hp.md` (`accepted` -> `running` -> `done`).
- Commit when code is ready. Hint: `cmd-037: WPF RemoteMicSender 60ms prefill cushion`
- **Do not push** unless Stew asks.

Full orders: `.mscc-coord/COMMANDS.yaml` id `cmd-037`.